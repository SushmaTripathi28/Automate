name: secrets

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Configure GCP Credentials
      uses: gcp-actions/configure-gcp-credentials@v1
      with:
        gcp-region: ${{ secrets.GCP_REGION }}
    - name: List S3 Buckets
      run: aws s3api list-buckets
